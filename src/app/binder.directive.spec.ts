import { BinderDirective } from './binder.directive';

describe('BinderDirective', () => {
  it('should create an instance', () => {
    const directive = new BinderDirective();
    expect(directive).toBeTruthy();
  });
});
